name: ci

on:
  push:
    branches: test

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: TEST1
        run: |
          ls -lsah
          echo "$SSH_USER"
        env:
          SSH_USER: ${{ secrets.NAME1 }}